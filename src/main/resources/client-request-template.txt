${cr.protocol} ${cr.method} ${cr.path} ? ${cr.queryParams.collect { k,v-> "$k=$v"}.join(', ') }
Headers:
<% cr.headers.each { h-> %>    ${h.headerName}: ${h.toArray()}
<% } %>
Cookies:
<% cr.cookies.each { n,c-> %>    ${n} (${c.domain}, ${c.path}): ${c.value}
<% } %>
Character-Encoding: ${cr.characterEncoding}
Content-type: ${cr.contentType}
Content-Length: ${cr.contentLength}
Content:
${cr.body}