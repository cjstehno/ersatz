plugins {
    id 'org.ajoberstar.git-publish' version '2.1.2'
}

gitPublish {
    repoUri = 'git@github.com:cjstehno/ersatz.git'
    branch = 'gh-pages'

    contents {
        from(file('build/site')) {
            into '.'
        }
    }
}